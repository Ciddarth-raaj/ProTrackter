import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
    headingText: {
        fontWeight: 'bold',
        fontSize: 30
    },
    tasksWrapper: {
        alignItems: 'center',
    },
    filterTitle: {
        fontSize: 16,
    },
    filterText: {
        fontSize: 16,
        marginLeft: 5,
        fontWeight: 'bold'
    }
})