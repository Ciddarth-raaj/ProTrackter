import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
    headingText: {
        fontWeight: 'bold',
        fontSize: 30
    },
    tasksWrapper: {
        alignItems: 'center'
    }
})